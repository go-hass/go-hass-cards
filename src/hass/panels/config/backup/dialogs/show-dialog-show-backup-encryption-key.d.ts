export interface ShowBackupEncryptionKeyDialogParams {
    currentKey: string;
}
export declare const showShowBackupEncryptionKeyDialog: (element: HTMLElement, params?: ShowBackupEncryptionKeyDialogParams) => Event;
