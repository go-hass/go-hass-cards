import type { HomeAssistant } from "../../types";
import type { Selector } from "../selector";
export declare const formatSelectorValue: (hass: HomeAssistant, value: any, selector?: Selector) => string;
